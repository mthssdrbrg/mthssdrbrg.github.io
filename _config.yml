permalink: /:year/:i_month/:i_day/:title
exclude: [Gemfile, Gemfile.lock, README.md]
paginate: 3
sass:
  style: :compressed
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
