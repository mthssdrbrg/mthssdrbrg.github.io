permalink: /:year/:i_month/:i_day/:title/
exclude: [Gemfile, Gemfile.lock, README.md, vendor]
paginate: 3
gems: [jekyll-paginate]
sass:
  style: :compressed
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
